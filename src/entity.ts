export default class Entity {
    x: number
    y: number
    speed: number

    constructor(x: number, y:number, speed: number){
        this.x = x
        this.y = y
        this.speed = speed
    }
}