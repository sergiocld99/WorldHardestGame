export default class Entity {
    x: number
    y: number
    speed: number
    size: number

    constructor(x: number, y:number, speed: number, size: number){
        this.x = x
        this.y = y
        this.speed = speed
        this.size = size
    }
}